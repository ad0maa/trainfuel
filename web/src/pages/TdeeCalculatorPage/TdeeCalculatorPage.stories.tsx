import type { Meta, StoryObj } from '@storybook/react'

import TdeeCalculatorPage from './TdeeCalculatorPage'

const meta: Meta<typeof TdeeCalculatorPage> = {
  component: TdeeCalculatorPage,
}

export default meta

type Story = StoryObj<typeof TdeeCalculatorPage>

export const Primary: Story = {}
